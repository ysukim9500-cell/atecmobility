# ═══════════════════════════════════════════════════════════════════════
#  ★ 이 리포지터리는 공개다(PUBLIC). 여기 올라가는 것은 누구나 볼 수 있다.
#
#  검증·미리보기용 부산물에는 직원 실명과 운행 주소가 그대로 들어 있다.
#  (예: _realdata.json = 운행 2,695건, 76MB / _v*.html = 실직원 이름)
#  파일 이름을 하나씩 적으면 새로 생긴 것을 놓친다 — 규칙으로 막는다.
# ═══════════════════════════════════════════════════════════════════════

# 밑줄로 시작하는 파일은 전부 작업용으로 본다. 배포할 것에는 밑줄을 쓰지 말 것.
_*

# CI 원본 자료(대용량 PNG·PDF). 실제로 쓰는 로고는 brand/ 에 있다.
ci/
ci.pdf

# 기타 부산물
server.log
*.log
node_modules/

# 앱이 내보낸 실제 서류(직원 실명·운행 주소가 들어 있다). 밑줄로 시작하지 않아
# 위 규칙에 안 걸린다 — 대조용으로 받아 둔 것이라 여기 올라가면 안 된다.
app_*.pdf
app_*.xlsx

# 배포본의 원본. 여기 두 벌이 생기면 어느 쪽이 진짜인지 알 수 없다.
driving-v2.html
